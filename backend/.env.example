# Base de données PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/signalement_citoyen?schema=public"

# Serveur
PORT=3000
NODE_ENV=development

# JWT (à changer en production)
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
